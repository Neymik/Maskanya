---
- name: reload nftables
  ansible.builtin.systemd:
    name: nftables
    state: reloaded

- name: reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: reload maskanya iptables
  ansible.builtin.systemd:
    name: maskanya-iptables.service
    state: restarted
